Wireshark integration for Hop.
===

# Quickstart

Copy `hop.lua` into `$HOME/.local/lib/wireshark/plugins` and launch wireshark
Run `tcpdump -i any -w outputfile.pcap` to get a packet dump

# TODO
Reliable Tube reassembly
Integrating crypto
