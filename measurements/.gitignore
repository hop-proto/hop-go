# we build the hop executable to prevent our tests from being skewed by rebuilding it
hop

# ignore the pty_time executable
pty_time/pty_time

# ignore data files generated by running the tests
latency.csv
